<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head", { title: "Login" }) %>
    <link rel="stylesheet" href="/css/listaProductos.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <%- include("./partials/header") %>
    </header>

    <main class="container">
        <h1 class="main-title">Editar Producto</h1>
            <form action="/product/editar-producto?_method=PUT" method="POST" class="product-form">
                <input type="hidden" name="productId" value="<%= product.id %>">
                <label for="productCategory">Categoría del Producto:</label>
                <input type="text"  name="productCategory" value="<%= product.category %>">
                <label for="productName">Nombre del Producto:</label>
                <input type="text"  name="productName" value="<%= product.name %>">
                <label for="productImage">Imagen del Producto:</label>
                <input type="file"  name="productImage">
                <label for="productPrice">Precio del Producto:</label>
                <input type="number"  name="productPrice" step="0.01" value="<%= product.price %>">
                <label for="productDiscounñt">Descuento del Producto:</label>
                <input type="number"  name="productDiscount" step="0.01" value="<%= product.discount %>">
                <label for="productStock">Stock del Producto:</label>
                <input type="number"  name="productStock" step="0.01" value="<%= product.stock %>">
                <label for="productDescription">Descripción del Producto:</label>
                <textarea  name="productDescription"><%= product.description %></textarea>
                <label for="productWarranty">Garantía del Producto:</label>
                <input type="number"  name="productWarranty" step="0.01" value="<%= product.warranty %>">
                <label for="productRating">Rating del Producto:</label>
                <input type="number"  name="productRating" step="0.01" value="<%= product.rating %>">
                <button type="submit" class="edit-product-btn">Guardar Cambios</button>
            </form>
    </main>

    <footer>
        <%- include("./partials/footer") %>
    </footer>
</body>
</html>